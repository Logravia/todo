const tasks = document.querySelector(".tasks")
const dates = document.querySelector(".dates")
const projects = document.querySelector(".projects")
const nav = document.querySelector("nav")


export {tasks, dates, projects, nav};
